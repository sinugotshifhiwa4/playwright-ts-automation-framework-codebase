{
  "compilerOptions": {
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",

    "lib": ["ES2022", "dom"],

    "strict": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    "resolveJsonModule": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    "types": ["node", "@playwright/test"],

    "noEmit": true,
    "sourceMap": true,
    "isolatedModules": true
  },

  // Must stay in sync with FILE_GROUPS.typed in src/config/eslint/constants.mjs.
  // A file ESLint type-checks but tsconfig does not include will fail to parse.
  // src/config/ and scripts/ hold .mjs tooling — linted by ESLint, never compiled.
  // They fall outside these globs anyway, which only match .ts.
  "include": ["src/**/*.ts", "tests/**/*.ts", "fixtures/**/*.ts", "*.ts"],

  "exclude": [
    "node_modules",
    "dist",
    "logs",
    "downloads",
    "playwright-report",
    "test-results"
  ]
}
